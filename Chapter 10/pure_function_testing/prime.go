package prime

// IsPrime checks if the given number n is prime.
// It is a pure function with no side effects.
func IsPrime(n int) bool {
    if n <= 1 {
        return false
    }
    for i := 2; i*i <= n; i++ {
        if n%i == 0 {
            return false
        }
    }
    return true
}
